# ==========================================================
# Sistema Operacional / Editor
# ==========================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Ícones no diretório (macOS)
Icon

# Arquivos temporários
._*
.Spotlight-V100
.Trashes

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Byebug / Pry
.byebug_history
.pry_history

# ==========================================================
# Ruby / Rails
# ==========================================================
/.bundle
/vendor/bundle
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep
/storage/*
!/storage/.keep

# Active Storage
/public/assets
/app/assets/builds/*
!/app/assets/builds/.keep

# Node
/node_modules
/yarn-error.log

# ==========================================================
# Configurações sensíveis
# ==========================================================
/config/master.key
/config/credentials/*.key
/config/settings.local.yml
/config/settings/*.local.yml
/config/environments/*.local.yml

# Arquivos de settings com segredos (mantidos só localmente)
config/settings/development.yml
config/settings/production.yml
config/settings/test.yml

# ==========================================================
# Banco de Dados
# ==========================================================
/db/*.sqlite3
/db/*.sqlite3-journal
/db/*.sqlite3-*

# Dump do schema (se usar migrations apenas)
# /db/schema.rb
/db/structure.sql

# ==========================================================
# Testes / Cobertura
# ==========================================================
/coverage/
/spec/examples.txt
/spec/tmp/*
/rspec_failures

# ==========================================================
# Outros
# ==========================================================
/tmp/*
*.bak
*.swp
*.swo
*.tmp
*.orig